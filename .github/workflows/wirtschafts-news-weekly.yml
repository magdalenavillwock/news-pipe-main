name: "Wirtschafts-News - Wochenrueckblick"
on:
  schedule:
    - cron: "0 6 * * 0"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    uses: ./.github/workflows/run-weekly.yml
    with:
      subscription: wirtschafts-news
    secrets: inherit

