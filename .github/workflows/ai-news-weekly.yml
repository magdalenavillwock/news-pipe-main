name: "AI & ML News — Wochenrückblick"
on:
  schedule:
    - cron: "0 17 * * 0"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    uses: ./.github/workflows/run-weekly.yml
    with:
      subscription: ai-news
    secrets: inherit
