name: "Bali Flug-Deals"
on:
  schedule:
    - cron: "0 6 * * *"
    - cron: "0 16 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    uses: ./.github/workflows/run-subscription.yml
    with:
      subscription: bali-flights
    secrets: inherit
