name: "AI & ML News"
on:
  schedule:
    - cron: "0 4 * * 1-5"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    uses: ./.github/workflows/run-subscription.yml
    with:
      subscription: ai-news
    secrets: inherit
